{
  "name": "storytel-server",
  "version": "1.0.0",
  "description": "Storytel Web Server",
  "main": "server.js",
  "scripts": {
    "start": "node dist/server.js",
    "dev": "nodemon server.js",
    "build": "node build.js",
    "build:prod": "NODE_ENV=production node build.js",
    "build:watch": "nodemon --exec \"node build.js\" --ext js --ignore dist/"
  },
  "dependencies": {
    "@fastify/cors": "^11.1.0",
    "@fastify/jwt": "^10.0.0",
    "axios": "^1.6.0",
    "crypto": "^1.0.1",
    "dotenv": "^17.2.2",
    "fastify": "^5.6.1"
  },
  "devDependencies": {
    "esbuild": "^0.19.12",
    "nodemon": "^3.0.1"
  }
}
